# Future localization data

Full 78-card content (Spanish and English), same JSON shape as the current
Polish `app/src/main/assets/cards.json`. Not wired into the app yet — kept
here for when EN/ES locale support gets implemented (system-locale
auto-detection is the preferred approach over a first-launch picker).
